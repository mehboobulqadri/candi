# Concepts

Deep dives that should NOT sit in the always-on context.

One file per concept (architecture, ADRs, big designs, complex subsystems).
Agents read exactly the file they need — never dump this dir into context.

Suggested format: what it is, why it exists, how it works, gotchas.
