[package]
name = "candi-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
candi-pdf = { path = "../candi-pdf" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
